```
amixer sset 'Master' #80%调节播放的音量没有反应
madplay -a +1 mnt/udisk/AUDIO/784\ MP3/02\ 1KHZ\,L\&R\,0DB.mp3 #调节播放音量有反应



	master是声卡中的一个逻辑控制单元（总开关），master在收到软件层面的命令时，会调用声卡物理芯片执行音量调节。声卡内部有数模转换器（DAC）和功率放大器，master通过改变放大器倍速改变声音输出的大小。
┌───────────────┐
│   用户操作    │  →  如 amixer 命令、音量滑块
└───────┬───────┘
        ↓
┌───────────────┐
│  Master 音量  │  →  声卡的“主控制节点”（软件可操作的逻辑单元）
└───────┬───────┘
        ↓
┌───────────────┐
│   声卡硬件    │  →  实际执行音量调节的物理芯片（如 RK3568 内置的音频 codec）
└───────┬───────┘
        ↓
┌───────────────┐
│  扬声器/耳机  │  →  输出最终声音
```





```
	模拟麦克风组成：振膜+音圈+磁体组成的换能结构
	工作流程：声音震动带动音圈在磁场中运动，产生与声音振动频率、幅度对应的模拟电信号，rk807中的ADC将模拟电信号转化为数字信号后，CPU对数字信号进行处理（录音，识别语音等）
	//若是模拟电信号微弱，需通过外部/或内置“前置放大器”提升信号幅度
	
	数字麦克风：振膜+音圈，前置放大器，内置ADC（模拟-数字转换器）、数字信号处理（DSP，可选，用于降噪、增益调节）、数字接口模块（I2S/PDM 为主）
```

![image-20250905143124168](C:\Users\Administrator\AppData\Roaming\Typora\typora-user-images\image-20250905143124168.png)



###### 基础参数

1.采样率：每秒钟采集/播放的音频样本数，Hz

​	8kHz（语音通话）/44.1kHz（CD 音质）/48kHz（常见于视频/多媒体）/96kHz/192kHz（Hi-Fi）

```
aplay -r 48000 test.wav
```



2.位深：表示一个样本点的数据精度，即每个样本多少位，16-bit = 每个点用 2 字节存储。

​	16-bit（最常见，int16_t）/24-bit（专业音频设备）/32-bit（浮点高精度处理）

​	位深越高，动态范围越大，数据量越大

```
arecord -f S16_LE /tmp/test_rec.wav
```



3.声道：音频数据的并行通道数

​	单声道（Mono，1 ch）/立体声（Stereo，2 ch）/多声道（5.1 / 7.1）

```
aplay -c 2 test.wav
```



4.缓冲区（Buffer）：ALSA的PCM播放/录音通过环形缓冲区管理

​	period：缓冲区被划分为多个period，每写满一个period，驱动触发一次中断

​	Buffer太小会卡顿破音，太大延迟增加

```
mpg123 -b 512 test.mp3
```



5.延迟（Latency）：应用层写入数据，到实际从扬声器、耳机播放出来的时间差

​	由DMA 缓冲区大小，I2S传输速率，Codec处理延迟决定

​	实时性高时（对讲机）要降低延迟



6.音频同步（Audio Sync）：多媒体系统中保持音频视频（A/V）对齐

​	音频时钟来源Codec/I2C的MCLK要稳定，系统根据采样率和时钟漂移做同步

​	播放器中常见AV Sync算法：音频快了丢包，慢了插入静音



**采样率** = 每秒采多少点。

**位深** = 每点精度。

**声道** = 有几路声音。

**缓冲区** = 数据暂存仓。

**延时** = 从写数据到听到的时间。

**同步** = 音视频对齐。



​	如果音频文件本身的采样率不等于指定的采样率，ALSA会尝试重采样（resample）。比如文件是 44.1kHz，你指定 48kHz 播放，ALSA lib 会先做插值处理再送到硬件。

**录音指定采样率** → 控制“ADC 采样的速度”。

**播放指定采样率** → 控制“DAC 播放的速度”。



###### ADC和DAC

​	Analog-to-Digital Converter（模数转换器）：把模拟声音信号转换成数字PCM信号

​		麦克风采集到连续的模拟信号（电压随时间变化），ADC通过采样率将其离散化，每秒取N个点，没个点用位深表示幅度大小，储存为数字，最后输出PCM数据流，交给I2S/DMA送到CPU

​		信噪比越高录音越干净

​	Digital-to-Analog Converter （数模转换器）

​		播放应用层传过来的PCM数据，DAC按采样率将数据恢复成连续的模拟电压波形，通过放大器AMP驱动扬声器/耳机

​		采样率支持范围（ 8kHz–192kHz），分辨率/位深(16-bit、24-bit、32-bit)，

​		**失真度** 越低越好

​		输出功率决定能否直接驱动耳机，还是要外置功放

录音链路：
Mic (模拟) → Codec ADC → I2S (数字) → CPU/内存 → ALSA → 应用层 (arecord)

播放链路：
应用层 (aplay) → ALSA → CPU/内存 → I2S (数字) → Codec DAC → Speaker/Headphone (模拟)



###### PCM是什么

​	PCM（Pulse Code Modulation，脉冲编码调制）：最原始的数字音频数据，描述了模拟声音信号在时间轴上的幅度值序列

​		PCM没有压缩，体积大音质好	WAV文件即带文件头的PCM数据

​		MP3/AAG/OGG:压缩格式，需解码后得到PCM后才能播放

​	具体意义取决于采样率，位深（16-bit），声道数（单声道Mono、立体声Stereo，多声道时 PCM 数据交错存放），编码格式（`S16_LE` → 有符号 16-bit 小端序···）
