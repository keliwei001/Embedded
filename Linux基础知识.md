###### 重定向

```
控制命令输入来源和输出去向的工具，默认时，一个命令通常有三个标准的数据流：
​	标准输入：键盘
​	标准输出：终端屏幕
​	标准错误：错误信息，默认也显示在终端屏幕

输出重定向：>和>>
	> :覆盖重定向，将命令的输出写入到一个文件，文件存在则清空后写入，不存在则创建
	>> : 追加重定向，将命令的输出追加到一个文件中，，不存在则创建

输入重定向：<
	< :告诉命令从一个文件中读取，而不是等待键盘输入

重定向标准错误：2>
	1代表标准输出，2代表标准错误
		# 假设一个不存在的命令 	non_existent_command 2> error.log
		该命令会执行失败， 2> error.log表示将错误信息重定向到error.log文件，终端屏幕不会显示错误信息
		
		ls -l > file_list.txt
		echo "New log entry" >> my_logfile.log
		date >> my_logfile.log
		sort < unsorted_list.txt
		my_script > output.log 2>&1		#将标准输出和标准错误都重定向到同一个文件
```

