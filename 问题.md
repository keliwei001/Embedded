问题

###### 01-menconfig中配置为什么没有生效？

我们在menuconfig中保存的.config每次编译的时候脚本都会组合生产.config,所以你的配置会被覆盖掉，应该生成defconfig后覆盖之前的。

在kernel中，要export ARCH=arm64 rockchip_linux_defconfig指向该配置文件



###### 02-一个引脚导出后无法拉高拉低

​	GPIO2_B7在wireless_bluetooth中设置为复位脚，但是在蓝牙开启后关闭了再重新打开出现问题，需要重新上电才能再次配置蓝牙。

```
&wireless_bluetooth {
	compatible = "bluetooth-platdata";
	clocks = <&rk809 1>;
	clock-names = "ext_clock";
	//wifi-bt-power-toggle;

	uart_rts_gpios = <&gpio2 RK_PB5 GPIO_ACTIVE_LOW>;

	pinctrl-names = "default", "rts_gpio";
	pinctrl-0 = <&uart1m0_rtsn>;
	pinctrl-1 = <&uart1_gpios>;
	BT,reset_gpio    = <&gpio2 RK_PB7 GPIO_ACTIVE_HIGH>;
	BT,wake_gpio     = <&gpio2 RK_PC1 GPIO_ACTIVE_HIGH>;
	BT,wake_host_irq = <&gpio2 RK_PC0 GPIO_ACTIVE_HIGH>;
	status = "okay";
};
```

​	验证：将该引脚置空，配置该引脚无法生效

```
echo 79 > /sys/class/gpio/export 
echo out > /sys/class/gpio/gpio79/direction
cat /sys/class/gpio/gpio79/value
cat /sys/kernel/debug/gpio
echo 1 > /sys/class/gpio/gpio79/value
echo 0 > /sys/class/gpio/gpio79/value
```

​	查看原理图或pinctrl.dtsi中的复用关系，发现gmac0节点中的status = "okay"，gmac0会先占用该引脚，后续Bluetooth无法正常复位。

![image-20251024102533397](C:\Users\Administrator\Desktop\md\问题.assets\image-20251024102533397.png)

```

&gmac0 {
	······
	phy-handle = <&rgmii_phy0>;
	status = "okay";
};
```

​	将gmac0中改为status = "disable"即可





###### 03-休眠唤醒后触摸失效

上电时显示触摸正常，休眠唤醒后显示正常触摸失效，lcd和触摸共用电源

```
	#示波器检测没有复位信号，i2c也没有，后续排查到驱动没有运行到
static void gtp_resume(struct goodix_ts_data *ts)
	
	#手动复位能恢复触摸
echo 0 > /sys/bus/i2c/devices/1-005d/drv_irq
echo 1 > /sys/bus/i2c/devices/1-005d/reset
echo 1 > /sys/bus/i2c/devices/1-005d/drv_irq
cat /sys/bus/i2c/devices/1-005d/workmode
cat /sys/bus/i2c/devices/1-005d/drv_irq

# 1. 禁用工作线程
root@rk3568-buildroot:/# echo 0 > /sys/bus/i2c/devices/1-005d/drv_irq
[ 1184.988252] goodix-ts 1-005d: Input report thread disabled!

# 2. 复位触摸芯片（关键步骤）
root@rk3568-buildroot:/# echo 1 > /sys/bus/i2c/devices/1-005d/reset
[ 1194.108083] goodix-ts 1-005d: Guitar reset

# 3. 重新启用工作线程
root@rk3568-buildroot:/# echo 1 > /sys/bus/i2c/devices/1-005d/drv_irq
[ 1200.171928] goodix-ts 1-005d: Input report thread enabled!

# 4. 检查状态
root@rk3568-buildroot:/# cat /sys/bus/i2c/devices/1-005d/workmode
in_work_mode
root@rk3568-buildroot:/# cat /sys/bus/i2c/devices/1-005d/drv_irq
enabled
root@rk3568-buildroot:/# 



#添加脚本
~/myproject/rk356x_linux5.1/buildroot/board/rockchip/rk3566_rk3568/fsoverlay/etc/pm/sleep.d/99-gt928-reset


修改触摸驱动gt9xx.c时
结构体成员 → 在 struct goodix_ts_data 定义中添加
设备ID表 → 在文件开头的全局变量区域添加
工作队列函数 → 在 gtp_work_func 函数附近添加
事件处理函数 → 在中断处理函数附近添加
初始化代码 → 在 gtp_probe 函数的合适位置添加
suspend修改 → 在 gtp_suspend 函数末尾添加
remove清理 → 在 gtp_drv_remove 函数中添加清理代码
这样添加后，当用户按下电源键唤醒设备时，触摸功能会自动复位并恢复正常工作。
```



























